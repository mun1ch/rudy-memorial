import { beforeAll, afterAll } from "vitest";

// Simple test setup - no complex configuration needed for now
beforeAll(() => {
  // Setup code if needed
});

afterAll(() => {
  // Cleanup code if needed
});
